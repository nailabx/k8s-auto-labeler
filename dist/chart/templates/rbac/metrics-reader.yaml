{{- if .Values.metrics.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    name: k8s-auto-labeler-metrics-reader
rules:
    - nonResourceURLs:
        - /metrics
      verbs:
        - get
{{- end }}
